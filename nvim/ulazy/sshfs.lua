-- https://github.com/uhs-robert/sshfs.nvim

--- @type LazyPluginSpec
return {
  "uhs-robert/sshfs.nvim",
	enabled = false, -- does not support Nushell as default
  opts = {
    -- Refer to the configuration section below
    -- or leave empty for defaults
  },
}
